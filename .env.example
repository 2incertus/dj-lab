# sha256 of the numeric PIN that gates the site. Leave empty to disable auth locally.
#   printf '0000' | sha256sum
PIN_HASH=

# taste engine, used to search Spotify for a track before you pick its YouTube match
TASTE_URL=http://host.docker.internal:8930
