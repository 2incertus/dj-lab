"""djlab -- local prototyping toolkit for Brazilian x techno blends.

Modules:
    audio    -- load/save, gain staging, fades
    analyze  -- BPM + key detection, warp-risk flags
    synth    -- techno sound generators + step sequencer
    beds     -- the 5 synthesized techno frames
    warp     -- half-time-aware time stretching
    mix      -- layering, sidechain ducking, master bus
    recipes  -- the 5 blend definitions (bed + source transforms)
"""

SR = 44100
